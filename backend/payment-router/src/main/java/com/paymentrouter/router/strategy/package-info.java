/**
 * Strategy Pattern.
 * One strategy per destination DFSP isolates DFSP-specific behaviour,
 * plus the factory that selects a strategy by provider code.
 */
package com.paymentrouter.router.strategy;
